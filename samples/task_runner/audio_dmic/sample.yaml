sample:
  description: Digital microphone task runner sample
  name: Digital microphone  task runner
common:
  filter: dt_alias_exists("dmic-dev") and dt_chosen_enabled("zephyr,bt-hci")
  build_only: true
  required_snippets:
    - infuse
tests:
  sample.task_runner.audio_dmic:
    tags: app
