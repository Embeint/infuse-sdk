# Infuse IoT SDK options

config INFUSE_SDK
	bool "Build application with Infuse IoT SDK"

rsource "lib/Kconfig"
rsource "subsys/Kconfig"

# Infuse IoT SDK defaults
rsource "Kconfig.defaults.core"
rsource "Kconfig.defaults.net"
rsource "Kconfig.defaults.nrf"
