# Override KV_STORE_KEY_ALG_TILT_ARGS dependencies

config KV_STORE_KEY_ALG_TILT_ARGS
	default y

config INFUSE_REBOOT
	default y

config INFUSE_REBOOT_COMPILE
	default n

config INFUSE_REBOOT_RETURN
	default y

source "Kconfig.zephyr"
