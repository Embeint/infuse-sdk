# Application Kconfig settings

# Include our updated definitions since CONFIG_INFUSE_DEFS_GENERATED_DOWNSTREAM=y
# Normally this would be done by the root downstream Kconfig file. The test
# generates these files in CMake before Zephyr evaluates Kconfig.
source "$(KCONFIG_BINARY_DIR)/generated/Kconfig.kv_keys"
source "$(KCONFIG_BINARY_DIR)/generated/Kconfig.rpc_commands"

# Source base Zephyr kconfig tree
source "Kconfig.zephyr"
