common:
  tags: data_logger
  min_flash: 64
  min_ram: 32
tests:
  data_logger.flash_map:
    platform_allow:
      - mps2/an385
      - qemu_cortex_m3
    integration_platforms:
      - qemu_cortex_m3
  data_logger.flash_map.large:
    platform_allow:
      - qemu_cortex_m3
    integration_platforms:
      - qemu_cortex_m3
    extra_args:
      - DTC_OVERLAY_FILE="large.overlay"
