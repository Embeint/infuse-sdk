common:
  tags: data_logger tdf
  min_flash: 64
  min_ram: 32
tests:
  data_logger.tdf.epacket:
    platform_allow:
      - mps2/an385
      - qemu_cortex_m3
    integration_platforms:
      - qemu_cortex_m3
  data_logger.tdf.epacket_footer:
    extra_args: DTC_OVERLAY_FILE="./app.overlay;./footer.overlay"
    extra_configs:
      - CONFIG_EPACKET_PACKET_SIZE_MAX=74
    platform_allow:
      - mps2/an385
      - qemu_cortex_m3
    integration_platforms:
      - qemu_cortex_m3
