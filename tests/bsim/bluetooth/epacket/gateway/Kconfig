# Test options

config INFUSE_REBOOT
	default y

config INFUSE_REBOOT_COMPILE
	default n

config INFUSE_REBOOT_RETURN
	default y

configdefault INFUSE_LITTLEFS_CACHE_SIZE
	default 1024

source "Kconfig.zephyr"
